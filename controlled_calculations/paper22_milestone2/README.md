# Paper 2.2 — Milestone 2 package

This package contains the frozen controlled-analysis protocol for the exactly
three systems from Milestone 1.  It deliberately contains no fitted DIPS model,
no baseline result, no optimisation, and no quantum calculation.

The next permitted action is implementation of the already-declared methods,
then one locked-test evaluation under `ANALYSIS_PROTOCOL_FROZEN.md`.
